export type Spot = {
  id: string;
  region: string;
  image: string;
  place: string;
  link: string;
};

export type RawSpot = {
  番号: number;
  所在地: string;
  スポット画像: string;
  場所名: string;
  地図: string;
};
